<?php

namespace App\Policies;

use App\Models\User;
use Illuminate\Auth\Access\Response;

class UserPolicy
{
    /**
     * Determine whether the user can view any models.
     */
    public function viewAny(User $user): bool
    {
        return false;
    }

    public function edit(User $authenticatedUser, User $userToEdit)
    {
        // Only allow editing if:
        // 1. User is editing themselves, OR
        // 2. User has permission to edit any user, AND
        // 3. User isn't trying to edit a super-admin (unless they're also super-admin)
        return $authenticatedUser->id === $userToEdit->id ||
            ($authenticatedUser->can('edit users') &&
                (!$userToEdit->hasRole('super-admin') || $authenticatedUser->hasRole('super-admin')));
    }

    /**
     * Determine whether the user can view the model.
     */
    public function view(User $user, User $model): bool
    {
        return false;
    }

    /**
     * Determine whether the user can create models.
     */
    public function create(User $user): bool
    {
        return false;
    }

    /**
     * Determine whether the user can update the model.
     */
    public function update(User $user, User $model): bool
    {
        return false;
    }

    /**
     * Determine whether the user can delete the model.
     */
    public function delete(User $user, User $model): bool
    {
        return false;
    }

    /**
     * Determine whether the user can restore the model.
     */
    public function restore(User $user, User $model): bool
    {
        return false;
    }

    /**
     * Determine whether the user can permanently delete the model.
     */
    public function forceDelete(User $user, User $model): bool
    {
        return false;
    }
}
